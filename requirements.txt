# 🚗 國道1號圓山-三重路段交通預測系統 - 依賴套件
# ================================================================

# 核心數據處理
numpy>=1.24.3
pandas>=2.0.3
scipy>=1.11.1

# 機器學習和AI模型
scikit-learn>=1.3.0
xgboost>=1.7.0
joblib>=1.3.0

# 深度學習 (可選 - 用於LSTM模型)
# 根據不同情況選擇以下其中一個：

# 選項1：最新版 TensorFlow (推薦)
tensorflow>=2.13.0,<2.17.0

# 選項2：如果遇到版本問題，使用穩定版本
# tensorflow==2.15.0

# 選項3：如果只有CPU，使用CPU版本
# tensorflow-cpu>=2.13.0,<2.17.0

# 選項4：如果TensorFlow有問題，可以單獨安裝Keras
# keras>=3.0.0

# 數據視覺化
matplotlib>=3.7.2
seaborn>=0.12.2
plotly>=5.15.0

# 數據解析和處理
lxml>=4.9.3
xmltodict>=0.13.0

# 系統監控和優化
psutil>=5.9.0

# 日期時間處理
python-dateutil>=2.8.2

# 進度條顯示
tqdm>=4.65.0

# JSON處理增強
ijson>=3.2.0

# 路徑處理
pathlib2>=2.3.7

# ================================================================
# 🔧 TensorFlow/Keras 安裝故障排除:
# ================================================================
# 
# 如果遇到導入錯誤，嘗試以下解決方案：
#
# 1. 重新安裝 TensorFlow:
#    pip uninstall tensorflow keras
#    pip install tensorflow==2.15.0
#
# 2. 使用 CPU 版本:
#    pip install tensorflow-cpu==2.15.0
#
# 3. 使用 conda 安裝:
#    conda install -c conda-forge tensorflow
#
# 4. 如果仍有問題，系統會自動回退到 XGBoost + 隨機森林
#
# ================================================================
# 📦 安裝說明:
# 
# 基礎安裝 (不含深度學習):
# pip install numpy pandas scipy scikit-learn xgboost matplotlib seaborn plotly lxml psutil tqdm
#
# 完整安裝 (含LSTM深度學習):
# pip install -r requirements.txt
#
# 測試安裝:
# python -c "import tensorflow; print('TensorFlow版本:', tensorflow.__version__)"
# ================================================================